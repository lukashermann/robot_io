from setuptools import setup

setup(name='robot_io',
      version='0.0.1',
      install_requires=['numpy',
                        'scipy',
                        'open3d',
                        'hydra.core']
)